/*
加入差值绝对值直到长度固定
给定义非负数组a,计算任意两数差值的绝对值
如果a中没有,就加入a中，但是只加入1份
然后对于新的数组a,继续计算任意两数差值的绝对值
如果a中没有,就加入a中，但是只加入1份
直到a长度不变，返回a最终的长度
 */
 //结论：令数组中全部非0数字的gcd为d，d到数组最大值之间，所有的d的倍数都会包含在稳定之后的数组中
 //使用辗转相减思考，令a>b,gcd(a,b)=gcd(a-b,b)，终止条件:a==b
 //这样一来，一直减一定能减出gcd，数组中所有数字都是gcd的倍数
 //假设最大值为k*d,有了d和k*d,就能够造出(k-1)*d,逐步构造即可得出所有d的倍数
 //下面证明不会有其他的数字：辗转相减的过程中保证减出来的数字都是gcd的倍数，不会有其他因数
 //注意点：0是边界条件，且数组中一个数字可能出现多次，我们需要讨论0是否存在以及0的个数