//给定一个非负数n，返回大于等于n的最小的
//2的幂次，保证在有符号整数范围内
int Near2Power(int n)
{
	if (n <= 0)
	{
		return 1;
	}
	n--;
	//这里n--防止n原本是2的幂次但是被计算为下一个2的幂次
	//使用倍增的思想，将n--后n最左侧1的右侧全部位置刷成1
	//最后+1即可
	n |= (n >> 1);
	n |= (n >> 2);
	n |= (n >> 4);
	n |= (n >> 8);
	n |= (n >> 16);
	return n + 1;
}